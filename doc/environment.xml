<appendix id="env_vars"><title>Environment Variables</title>
<para>
A number of environment variables are available to control the
functionality of MRNet.
</para>

<informaltable frame="top">
<tgroup cols="2">
<colspec colname="col2" colnum="2" rowsep="1"/>
<tbody>
<row>
<entry>XPLAT_RSH</entry>
<entry>Set this variable to the name of the remote shell program to use for
remote process execution. Default is 'ssh'.</entry>
</row>
<row>
<entry>XPLAT_REMCMD</entry>
<entry>If it is necessary to run the remote shell program (XPLAT_RSH) with a 
utility such as <filename>runauth</filename> to non-interactively authenticate 
the unattended remote process, that command may be specified using this variable.</entry>
</row>
<row>
<entry>XPLAT_RESOLVE_HOSTS</entry>
<entry>Tell XPlat to perform DNS resolution of hostnames and IP addresses by
setting the variable to '1'. Default is '1'.</entry>
</row>
<row>
<entry>XPLAT_RESOLVE_CANONICAL</entry>
<entry>When XPLAT_RESOLVE_HOSTS is '1', setting this variable to '1' will
tell XPlat to try to resolve all hostnames to their canonical DNS format. Default
is '0'.</entry>
</row>
<row>
<entry>MRNET_OUTPUT_LEVEL</entry>
<entry>Set the debug output level (valid values are 1-5, default is 1). Level 1 will
only log warning/error messages, level 3 provides fairly detailed function execution logging,
and level 5 will produce every log message that MRNet generates.</entry>
</row>
<row>
<entry>MRNET_DEBUG_LOG_DIRECTORY</entry>
<entry>Specify the absolute path to the directory to store MRNet log files generated by
increasing MRNET_OUTPUT_LEVEL. If not set, the first existing directory from the following
list is used:<itemizedlist>
<listitem> <filename>$HOME/mrnet-log</filename> </listitem>
<listitem> <filename>$HOME</filename> </listitem>
<listitem> <filename>/tmp</filename> </listitem>
</itemizedlist></entry>
</row>
<row>
<entry>MRN_COMM_PATH</entry>
<entry>If <filename>mrnet_commnode</filename> is not in your path by default, 
you can specify the full path using this variable.</entry>
</row>
</tbody>
</tgroup>
</informaltable>

</appendix>
